{
    "tasks": [
        {
            "label": "f9",
            "type": "shell",
            "command":"scfg",
            "options": {
                "cwd": "${fileDirname}"
            },
            "args": [
                "${fileBasename}"
            ]
        }
    ],
    "version": "2.0.0"
}